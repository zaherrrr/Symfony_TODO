<form method="post" action="{{ path('app_club_delete', {'REF': club.REF}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ club.REF) }}">
    <button class="btn">Delete</button>
</form>
